require 'rails_helper'

RSpec.describe "DisDoseformDoseunitLinks", type: :request do
  describe "GET /dis_doseform_doseunit_links" do
    it "works! (now write some real specs)" do
      get dis_doseform_doseunit_links_path
      expect(response).to have_http_status(200)
    end
  end
end
